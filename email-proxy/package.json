{
  "name": "smtp-resend-proxy",
  "version": "1.0.0",
  "description": "SMTP proxy that converts SMTP to Resend API calls",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "smtp-server": "^3.12.0",
    "resend": "^3.0.0",
    "mailparser": "^3.6.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}

